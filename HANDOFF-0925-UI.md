# 0925 交接：由評分導向改為歌唱／錄音／後製導向

更新日期：2026-09-25（Asia/Taipei）。這份文件是下一個對話的接續入口。

## 1. 本次工作與不可改變的原則

使用者的最新指示：

> 所有現有功能都存在且不變；主流程由評分為主，轉成歌唱／錄音／後製為主，進行 UI 重新分配。

- 本輪只建立「0925 重要還原點（評分為主）」及交接文件，**尚未開始 UI 改版**。
- 下一輪是資訊架構、版面與操作入口重整；不能藉此刪除 K 歌、評分、歌曲庫、分離模式、診斷或舊資料相容性。
- 保持現有錄音／評分／混音／儲存行為與預設。不要順便換模型、改音量算法、調評分門檻或重新啟用 MIDI。
- 介面折疊與導覽切換不等於取消工作、停止收音或重新初始化播放器。
- 程式測試通過不是使用者驗收。使用者強調：**要他自己照平常操作流程也正常，才算解決**。
- 不猜測硬體或音質問題；先用實際紀錄定位，清楚區分已證實、推論與未知。無音訊感知工具時，不可聲稱「我聽過」。

## 2. 重要還原點

- 名称：**0925 重要還原點（評分為主）**。
- Git 標籤：`restore-important-20260925-scoring`。
- 功能程式基準 commit：`3b7e394847f08df040fe702184477ab5b9411fe5`。
- 標籤會指向包含本交接文件的文件提交；程式功能與上述 commit 相同。
- 網頁 build：`d70da7b75d21ade0`，上述功能提交的 GitHub Pages 部署已成功。
- 備份目錄：`D:\kala_ok_iphone\.runtime\restore-points\restore-important-20260925-scoring`。
- 內容：Git 完整歷史 bundle、此標籤原始碼 ZIP、交接文件副本、本機歌曲庫路徑設定、Python 套件清單、manifest、校驗碼與 RESTORE.md。
- **是程式還原點，不是 Windows 系統還原，也不包含音檔資料備份。**
- 不包含 `D:\歌單` 的歌曲、基準、遮罩、錄音，或大型模型、執行環境、瀏覽器設定與 Windows 音效設定。這些既有資料不搬動、不刪除。
- 安全還原指令與完整 commit 以備份內 `RESTORE.md`、`manifest.json` 為準。先在新目錄還原，不要直接 reset/清除現有工作區。
- 更早的 `restore-important-20260925-101257` 是上午的舊還原點，不含後來的分離預設與 IPv6 修正；本次應以 `...-scoring` 為基準。

## 3. 環境與啟動

| 項目 | 目前位置／內容 |
|---|---|
| 專案 | `D:\kala_ok_iphone` |
| Git | `main`，origin `https://github.com/nilson0606/kala-ok-iphone.git` |
| 使用者頁面 | https://nilson0606.github.io/kala-ok-iphone/ |
| 本機工具 | `http://127.0.0.1:4174`；Node helper，受 Host、Origin、session token 限制 |
| 本機開發頁 | `node server.mjs`，預設 4173 |
| 歌曲庫 | `D:\歌單`，可由 UI 變更，不可把此開發機路徑寫死成所有人的預設 |
| 錄音 | `D:\歌單\錄音\<uuid>\` |
| 路徑設定 | `.runtime/library-settings.json` |
| Python | `.runtime/venv/Scripts/python.exe`，Python 3.12 |
| 模型 | `.runtime/models` 下分模型保存，不在歌曲庫 |
| 硬體 | Windows、NVIDIA RTX 4060 Laptop GPU；桌機 Chrome／Edge 為主要環境 |

重開機後只需啟動，不需重裝：

```powershell
cd D:\kala_ok_iphone
powershell -NoProfile -ExecutionPolicy Bypass -File .\start-local.ps1
```

回網頁按「檢查本機工具」。停止使用 `stop-local.ps1`；只在安裝／依賴更新時使用 `setup-local.ps1`。啟動腳本可帶 `-NoOpen`。重啟 helper 前確認沒有下載、分離、錄音；記憶體工作會清除，已保存歌曲保留，可從歌曲庫重新載入。

## 4. 現有功能清單與預設（改版後都要保留）

### 歌曲、分離與試聽

- YouTube 網址載入；歌曲庫點歌也會載入相應影片。
- 分離設定預設收起。
- 預設：**完整歌曲（最長 15 分鐘）、RMVPE、Demucs／htdemucs、單次分離、人聲／伴奏、勾選保留試聽音軌**。
- 可選前 15／30／60 秒；可選 YIN／RMVPE。
- 模型保留 Demucs、BS-RoFormer／Viperx 1297、Mel-Band RoFormer／Kim。
- 流程保留單次、伴奏二次分離＋反向相減；相減用原始混音減第二輪伴奏。
- 模式保留全部人聲／伴奏與主唱／和音；後者用估計主唱建立基準。
- 兩軌／四軌試聽、缺音軌補建、進度／GPU狀態、取消／卸載、重新分離覆蓋、歌曲庫刪除皆保留。
- 分離百分比 100% 不等於準備完成；必須音軌驗證、基準與保存完成才就緒。
- 成功才替換指定版本；失敗產物不留下，不能破壞舊成功版本。
- 跨分離模型、處理流程、YIN／RMVPE、人聲模式的版本分開保存。載入舊歌會顯示其實際設定，不強制套新預設。
- 遮罩以**同一影片、同一分析範圍**共用，換模型／流程／音高／人聲模式或重做都沿用。遮罩只影響計分與參考顯示，不裁音、不靜音。

### K 歌、收音與評分

- YouTube 播放、麥克風音量表、即時音符／音高圖、原唱參考線、休息提示、手動／自動節拍燈、五音延遲測試皆保留。
- 「從頭開始唱」目前需要已載入歌曲基準，會處理麥克風啟動與 YouTube 歸零同步，成功後捲到播放器。UI 改版不能偷偷移除此前置條件；純錄音免基準若要做是另外的功能需求。
- 開麥本身不自動建立錄音；直接按 YouTube 播放可計分，但不等於開始保存錄音。
- 標準（預設）／嚴格／寬鬆、容許八度（預設）／嚴格原調、已演唱範圍（預設）／全曲都保留。
- 評分時間補償預設 **+150ms**，歌手時間往前比對，例如10.150秒歌聲對10.000秒基準。
- 音準60%、進拍25%、完整度15%，不因版面重整改算法。
- 結束並結算、播完自動結算、停止收音後仍可結算、歷史分數勾選刪除／全刪皆保留。
- 停止收音目前會暫停影片並回到0秒；外部頁面切背景會停止收音。應區分應用內切換 UI 分區和真的切離瀏覽器分頁，避免改版意外觸發清理。

### 錄音

- 內容選項：「歌唱者」（預設）、「歌唱者＋配樂／和音」、「不保存錄音」。瀏覽器會記住使用者選擇，不能改版時清掉。
- 「從頭開始唱」後影片播放時同步開始錄製；暫停／緩衝會暫停；跳轉依實際演唱顺序保存。
- 停止收音、結束並結算或影片播完會保存；重唱先保存上一段，再另開，不覆蓋。
- 歌唱者＋配樂／和音：兩軌版本加伴奏，四軌版本加伴奏＋和音；**不得混入原唱主唱或全部人聲**。配樂與和音共用音量調整。
- 音量預設自動平衡；可選「手動比例＋自動微調」。錄製前設定，不能為改UI變動信號路由／混音增益。
- 錄音歌聲校正預設 **+150ms**，成品歌聲提前，配樂不動。與評分補償分開，不能相加成300ms。預設檔名後綴 `+150ms`。
- 原始乾淨歌聲、播放段落／時間資料、基準／遮罩／設定保存供後製；錄製中先暫存，完成搬至本機錄音目錄。
- 錄音清單試聽、下載、單筆刪除／全部刪除；儲存失敗可下載救援音檔。保留未完成與舊錄音處理。
- 既有瀏覽器錄音搬存，確認成功才刪舊副本。不同瀏覽器接同一本機工具可共用錄音。

### 錄音後處理／後製

- 選錄音，調總延時，可分別「重新評分」及「重新合成」，避免重唱。
- 正值提前歌聲，負值延後，從原始資料重算，**不累加**。
- 重評可用錄音當時基準，或已載入的同影片／同分析範圍其他基準；可另選嚴格／標準／寬鬆，保留錄音當時遮罩、八度與範圍。
- 重評以保存的乾淨歌聲重新分析；原始分數紀錄不覆蓋。不可用套效果後聲音偷偷取代評分來源。
- 柔化：關閉（預設）、輕度、中度、強烈；使用者要的是可明顯聽到的選用音色效果，不是強制修復聲音。
- **MIDI／合成器功能已依使用者要求移除**。舊成品及舊標籤相容仍保留；不要重加。
- 重合成另存 WAV，帶 parentId 等資訊；原錄音保留。配樂來自當次歌曲庫版本。
- 現有重合成沿用原錄音音量平衡設定；後製獨立音量滑桿是尚未實作的新功能，不能寫成交付現況。
- 後製成品通常沒有獨立 raw/post 來源；可播放／轉MP3，但要再次處理需選回來源錄音。舊資料無來源也不可假裝能編輯。
- 轉 MP3（本機 FFmpeg 192kbps）並下載，同時存入錄音目錄；錄音診斷 JSON 匯出保留。

## 5. 最近問題與確定狀態

### IPv6 導致下載等待

- 使用者刻意刪除已保存歌曲重現，所以不可用快取回避問題。
- 實際追蹤原 yt-dlp 逐一嘗試8個 IPv6，每個15秒逾時，120秒後才改IPv4；後續API主機又可能同樣等待。
- 獨立 curl 測試 YouTube／Google／Cloudflare，IPv6都10秒逾時，IPv4皆成功（約0.8–0.9秒完整HTTPS）。本機到IPv6閘道正常，但不能定位故障在哪一跳。
- 修正：每次下載有**2秒**IPv6連線前測；未通過自動IPv4，無需使用者取消／重按。IPv6下載時連線類錯誤可再改IPv4一次；403/429或登入限制不誤判為IPv6故障。
- 使用者曾要求10秒，後來明確要求改回2秒；**最終為2秒**。
- 使用者之後回報「重開有連上IPv6」，並確認恢復。記錄為重開後恢復，不宣称已證明是Windows或Google哪端故障。
- 自動切換仍保留，不修改Windows網路設定。診斷詳情：`.runtime/recovery-2026-09-24/下載等待診斷-20260925.md`。

### 麥克風與伴奏

- 使用者輪流用兩套設備，已找到各自正常的對應設定。
- 其中舊耳麥需「本機相容收音＋系統預設麥克風」；明確指定該耳麥反而異常。不能強迫一律選實體装置，也不能宣稱綁定硬體。
- 先前瀏覽器直接開麥會讓伴奏消失／變小；無分析的getUserMedia也重現。Python本機收音是有效相容方案，使用者確認可用。
- 保留瀏覽器收音與本機相容收音選項；不得為UI工作改動其路由、裝置選擇記憶或權限流程。
- 原生模式：Python SoundCard/WASAPI→localhost串流→AudioWorklet→現有分析與錄音。
- 先前 pop 聲調查使用者已撤回，認為是自己音色被較好麥克風捕捉；不要繼續當作待修bug。
- `.runtime/pending-pop-fix-20260924` 是**未發布且已撤回的實驗備份**，不要套回程式。

### 評分與使用者體感

- 使用者更重視唱完的聽感，覺得分數與體感差距大，無法確認參考旋律是否正確。
- 已知算法限制：進拍是旋律起音匹配，還含音高條件；完整度是有效音高覆蓋，不等於歌詞唱完；基準可能受樂器／和音干擾。
- 《春夏秋冬》曾出現175ms比0ms聽起來準、重評卻較低。不能宣稱調UI或改難度已解決評分可靠性。
- 本次維持評分行為，只降低版面優先順序，讓使用者可選擇查看；不要以灌高分當作修正。

## 6. 下一版 UI 方向（尚未實作）

核心動線：**選歌 → 試麥／設定錄音 → 歌唱與錄音 → 聽這次錄音 → 後製 → 比較／匯出**。

建議以三個主要入口組織：

| 主要區域 | 主內容 | 次要／可收合內容 |
|---|---|---|
| 歌唱／錄音 | 選歌入口、播放器、開關麥、音量、錄音內容、開始／結束、錄音狀態與保存結果 | 裝置資訊、音量平衡、錄音延時、音高圖、評分設定／結果 |
| 我的錄音 | 找錄音、試聽、後製入口、下載、刪除 | 儲存／搬存說明、救援、全部刪除 |
| 錄音後製 | 選擇來源、延時、柔化、重合成、成品試聽、MP3 | 重評難度／基準、診斷匯出 |

歌曲庫／分離與音高設定、本機工具／路徑、操作手冊保留清楚入口。冷／暖配色與使用者記憶保留。模型說明等長文可收合，錯誤與進行中的工作不可藏到看不到。

實作優先順序：

1. 先完整盤點現有控制項與事件，把錄音操作移到播放器附近，把評分放到清楚可展開的區域；主要功能可直接到達。
2. 錄製中清楚顯示狀態，保存完成提供「聽這次錄音／前往後製」，自動對應正確錄音ID；不啟動自動播放，也不覆蓋使用者正處理的其他錄音。
3. 再評估原始錄音與衍生成品分組，原始來源容易找，避免使用者選到不可編輯成品後不知道回哪裡。分組只能是呈現，不能搬移／改寫資料或造成連動刪除。
4. 同位置 A/B 試聽及片段循環是先前提出的改進想法，**目前沒有實作**。屬新增互動，可在主版面穩定後再做；不能把它列成現有功能。若做要核對校正時間軸、暫停／跳轉段落、原成品延時不同等情況，不能只設定相同播放器秒數就宣称準確對齊。
5. 手冊同步更新到使用者可按步驟操作的真實畫面。

以上三區名稱、分頁或單頁布局可依空間再設計；使用者明確同意的是重心與功能不變，不是已批准每一個新互動。避免突然要求重建歌曲庫、重唱或重設兩套硬體。

## 7. 程式入口與改版風險

| 檔案 | 主要責任 |
|---|---|
| `index.html`、`style.css`、`theme.mjs` | 頁面與冷／暖配色 |
| `navigation.mjs` | 目前是單頁錨點導覽，會展開目標details並捲動；不是工作區分頁系統 |
| `app.mjs` | 麥克風、播放器、音高顯示、生命週期 |
| `session.mjs` | 準備／載入／演唱／結算狀態、控制項啟停、進度 |
| `recording.mjs` | 錄音生命週期、清單與保存／刪除 |
| `recording-post.mjs` | 後製選取、重評、重合成、MP3、診斷 |
| `recording-process.mjs`、`recording-mix.mjs`、`recording-soften.mjs` | 延時／離線合成、混音平衡、柔化 |
| `recording-tune.mjs` | 僅舊成品標籤相容，沒有MIDI生成 |
| `recording-store.mjs`、`recording-archive.mjs` | 瀏覽器暫存與本機錄音資料 |
| `scoring.mjs`、`mask-editor.mjs`、`calibration.mjs` | 計分／遮罩／延時測試 |
| `helper-local.mjs`、`local-jobs.mjs`、`local-library.mjs` | 本機服務、工作與歌曲庫 |
| `tools/audio_pipeline.py`、`tools/download_network.py` | 下載／分離／基準管線、IPv6自動回退 |
| `native-microphone*.mjs`、`native-mic-worklet.mjs`、`tools/native_microphone.py` | 已能正常使用的相容收音，UI改版盡量不動 |
| `manual.html`、`build-site.mjs` | 使用手冊、不可變版本資產打包 |

- 目前大量事件用固定ID查找DOM。搬動可保留ID，但不可複製成兩個同ID按鈕；要共用操作須走同一狀態邏輯。
- 不要移除／重建正在播放的YouTube iframe，或使切換UI觸發收音清理。
- 不要把「保存完成」與「只停止／尚在寫檔」混淆；計分狀態與錄音狀態不同步時需維持各自訊息。
- 錄音成品需保留parentId、延時後綴、柔化／舊效果標籤；孤立成品、已刪來源、未完成錄音要可辨識。
- 新增前端模組要加入 `build-site.mjs` assets，避免Pages更新後跨版本模組／404。
- `README.md` 有部分歷史敘述尚未同步（例如未錄音版本的麥克風不寫檔、裝置變更補償歸零等），不得拿它覆蓋現行行為；以目前程式、HTML手冊及本交接的最終使用者決定為準。

## 8. 驗證與交付

依變動執行適用檢查：

```powershell
npm test
node tests/recording-browser-check.mjs
node tests/recording-archive-browser-check.mjs
node build-site.mjs
```

Python 網路修正已有7個測試與既有12個pipeline測試通過：

```powershell
.runtime/venv/Scripts/python.exe -m unittest discover -s tools -p test_download_network.py
.runtime/venv/Scripts/python.exe -m unittest discover -s tools -p test_audio_pipeline.py
```

- UI改版需實際檢查桌機與窄螢幕，以及冷／暖主題、鍵盤焦點、長歌名、空清單、錯誤／忙碌狀態。若改DOM，既有browser測試需按新的真實入口調整，不能用跳過流程讓測試通過。
- 瀏覽器錄音測試有假麥克風／假播放器及本機接口fixture，可驗證保存、暫停、跳轉、混音、重評、柔化、下載、刪除、來源不變等；不是使用者實際硬體與聽感驗收。
- 下一輪核心驗收：歌曲庫載入→試麥→設定錄音→從頭唱→停止並保存→精確選到最新錄音→原音試聽→同來源調延時／柔化另存→成品試聽→MP3下載→刪指定版本不影響其他資料。
- 也要確認評分設定／分數／遮罩／重評／分離所有選項仍可用，既有來源依賴與錄音選項不變。
- 真實長時間、高負載歌曲由使用者試；不要為UI小改反覆跑昂貴模型。
- 原則：每次交付說明改了哪個操作、做過哪些實測、尚待使用者確認什麼；不要把「程式沒有報錯」說成整體已修好。

## 9. 給新對話的可貼文字

> 請先讀取 `D:\kala_ok_iphone\HANDOFF-0925-UI.md`。從 `restore-important-20260925-scoring` 的0925重要還原點接續，把UI重心改為歌唱／錄音／後製；所有現有功能、算法、資料與使用者設定保持不變，K歌與評分仍保留。先依交接文件盤點與重排現有入口，不要重做歌曲庫、改音訊路由、重新加入MIDI或回復已撤回的pop實驗。同步更新操作手冊，並用我實際的錄唱至後製流程驗收。
